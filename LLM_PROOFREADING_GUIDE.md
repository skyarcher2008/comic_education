# LLM大小写校对功能使用指南

## 功能概述

在翻译完成后、嵌入漫画气泡前，新增了LLM大小写校对步骤，专门用于规范英文文本的大小写格式。

## 校对规则

1. **句首大写**：每句话的第一个字母自动大写
2. **专有名词**：人名、地名、品牌名等专有名词首字母大写
3. **缩写词**：保持缩写词大写（如 "OK", "TV", "AI" 等）
4. **感叹词**：保持感叹词原样（如 "Oh!", "Ah!", "Hmm..."）
5. **只修正大小写**：不改变单词内容，不增删文本

## 使用方法

### 1. 启用校对功能

在左侧翻译设置面板中：
- 找到"AI模型设置"部分
- 勾选"启用大小写校对"选项

### 2. 设置目标语言

- 确保目标语言设置为"英语"或"English"
- 校对功能仅对英文翻译生效

### 3. 进行翻译

- 单张翻译：点击"翻译当前图片"
- 批量翻译：点击"翻译全部图片"
- 校对会在翻译完成后自动执行

## 校对示例

### 输入（翻译结果）
```
hello world! this is JOHN speaking.
OH NO! the TV is broken again!
i love NEW YORK city!
```

### 输出（校对后）
```
Hello world! This is John speaking.
Oh no! The TV is broken again!
I love New York City!
```

## 技术实现

### 处理流程
1. 用户勾选"启用大小写校对"
2. 前端传递 `enable_proofreading=true` 参数
3. 后端完成翻译后调用校对函数
4. 校对结果替换原翻译文本
5. 渲染到漫画气泡中

### 模型配置
- **默认配置**：使用与翻译相同的API配置
- **自定义配置**：未来可支持独立的校对模型配置
- **请求频率**：遵循翻译API的rpm限制

## 注意事项

1. **API消耗**：校对功能会增加额外的API调用
2. **处理时间**：会略微增加翻译总时间
3. **语言限制**：目前仅支持英文校对
4. **网络要求**：需要稳定的网络连接
5. **模型依赖**：校对质量取决于所使用的LLM模型

## 故障排除

### 校对不生效
- 检查目标语言是否设置为英文
- 确认"启用大小写校对"已勾选
- 查看控制台是否有错误信息

### 校对结果不理想
- 尝试更换更好的LLM模型
- 检查API密钥是否有效
- 确认网络连接稳定

### 处理速度慢
- 检查API的rpm限制设置
- 考虑使用响应更快的模型
- 减少批量翻译的并发数

## 文件修改记录

- `src/app/templates/index.html`：增加校对开关UI
- `src/app/static/js/main.js`：增加校对参数传递
- `src/app/api/translate_api.py`：增加校对参数解析
- `src/core/processing.py`：集成校对处理流程
- `src/core/proofreading.py`：校对功能实现

## 更新日志

- **v1.0** (2025-06-29)：初始版本，支持基本的英文大小写校对
- 计划添加更多语言支持
- 计划添加独立的校对模型配置
- 计划添加校对结果预览功能
